JWT_SECRET=your-jwt-secret
ALLOWED_ORIGINS=http://localhost:5173,https://journals.one
SQLITE_CACHE_SIZE=-128000
# The API's public URL, the same value as baseURL in Web-UI/config.js.
# Only the MCP sidecar reads it, for file uploads.
JOURNALS_PUBLIC_API_URL=http://localhost:9900
